spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/games_membership?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.Mysql8Dialect
    defer-datasource-initialization: true

  sql:
    init:
      mode: always
      schema-locations:
      data-locations: classpath:dml.sql

  servlet:
    multipart:
      maxFileSize: 50MB
      maxRequestSize: 50MB

  main:
    allow-bean-definition-overriding: true

logging:
  level:
    com:
      tutorial:
        spring-boot: DEBUG

  thymeleaf:
    cache: false

  file:
    path: logs

webhook:
  slack:
    url: https://hooks.slack.com/services/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

#    지원자 웹훅
#    url: https://hooks.slack.com/services/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
